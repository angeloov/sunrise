{
	"name": "sunrise-backend",
	"type": "module",
	"dependencies": {
		"@types/dotenv": "^8.2.0",
		"apollo-server-express": "^3.0.2",
		"cors": "^2.8.5",
		"dotenv": "^10.0.0",
		"express": "^4.17.1",
		"graphql": "^15.5.1",
		"nodemon": "^2.0.12"
	},
	"scripts": {
		"start": "tsc && node dist/server.js",
		"build": "tsc -w",
		"watch": "tsc -w",
		"dev": "nodemon -w ./ ts,json --exec \"node dist/server.js\"",
		"test": "jest"
	}
}
